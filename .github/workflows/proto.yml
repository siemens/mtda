# ---------------------------------------------------------------------------
# Verify mtda.proto is valid and the build step can generate stubs from it
# ---------------------------------------------------------------------------
#
# This software is a part of MTDA.
# Copyright (C) 2026 Siemens AG
#
# ---------------------------------------------------------------------------
# SPDX-License-Identifier: MIT
# ---------------------------------------------------------------------------

name: Proto stubs

on:
  pull_request:
    paths:
      - 'mtda/grpc/mtda.proto'

jobs:
  check-stubs:
    name: Verify stubs can be generated from mtda.proto
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Generate stubs
        run: uv run python scripts/generate-grpc-stubs.py --force

      - name: Verify generated files exist
        run: |
          test -f mtda/grpc/mtda_pb2.py
          test -f mtda/grpc/mtda_pb2_grpc.py
